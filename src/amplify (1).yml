version: 1
frontend:
  buildpath:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: publi
  cache:
    paths:
        - node_modules/**/*

 
