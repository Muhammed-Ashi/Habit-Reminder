version: 1
applications:
  - frontend:
      phases:
        build:
      commands:
        - npm run build
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: public
