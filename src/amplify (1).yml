version: 1
applications:
  - frontend:
      phases:
        build:
      commands:
        - npm run build
      artifacts:
        baseDirectory: Build
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: src
