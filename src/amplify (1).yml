version: 1
frontend:
  buildpath:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
  cache:
    paths:
        - node_modules/**/*

  artifacts:
 
    baseDirectory: build
